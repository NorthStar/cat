<h1>Listing Booklists</h1>
<table>
  <tr>
    <th>ID</th>
    <th>Title</th>
    <th>Intro</th>
    <th>link</th>
  </tr>
 
  <% @booklists.each do |booklist| %>
    <tr>
      <td><%= booklist.id %></td>
      <td><%= booklist.title %></td>
      <td><%= booklist.intro %></td>
      <td><%= link_to "Detail",department_booklist_path(:id =>booklist.id) %></td>
    </tr>
  <% end %>
</table>